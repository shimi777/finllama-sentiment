transformers>=4.44.0
accelerate>=0.33.0
bitsandbytes>=0.43.0
torch>=2.3.0
datasets>=2.20.0
requests>=2.31.0
huggingface_hub>=0.24.0
scikit-learn>=1.4.0
scipy>=1.11,<1.17  # scipy 1.17 array-api probe breaks sklearn import when torch>=2.11 is installed
pandas>=2.2.0
numpy>=1.26.0
matplotlib>=3.8.0
seaborn>=0.13.0
pyyaml>=6.0
tqdm>=4.66.0
vaderSentiment>=3.3.2
jupyter>=1.0.0

# --- NER pipeline (optional extras) ---
# seqeval: strict span-F1 (BIO/IOB2). Falls back to a hand-rolled implementation if absent.
seqeval>=1.2.2
# Streamlit + altair for the NER dashboard
streamlit>=1.34.0
# Local zero-shot NER baseline. CPU-friendly.
# gliner>=0.2.0
# API clients — only required if you actually run that backend.
# openai>=1.40.0
# anthropic>=0.40.0
# google-genai>=0.5.0
